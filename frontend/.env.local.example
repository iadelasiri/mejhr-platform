NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_NAME=Mejhr

# Used only by server-side (SSR) fetches. Leave unset for local non-Docker
# dev (falls back to NEXT_PUBLIC_API_URL above). In Docker Compose this is
# set to http://backend:8000 since "localhost" inside the frontend
# container does not reach the backend container.
# API_INTERNAL_URL=http://backend:8000
